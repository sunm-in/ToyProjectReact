import React from "react";
import styled from "styled-components";

const Text = (props) => {

    return(
        <React.Fragment>
            
        </React.Fragment>
    );
}

Text.defaultProps = {
    children: null,
    bold: false,
    color: "#f1f2f6",
    size: "14px",
}

export default Text;