import React from "react";
import styled from "styled-components";

const Button = (props) => {

    return(
        <React.Fragment>

        </React.Fragment>
    );
}

Button.defaultProps = {
    
}

export default Button;